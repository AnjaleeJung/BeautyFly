<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".FragmentBookingPendingAdmin">
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/fragment_pending_booking_recycler_admin"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:background="@drawable/salonbggg"
        />
    <ImageView
        android:id="@+id/booking_noItemsFoundImgPendingAdmin"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:visibility="gone"
        android:src="@drawable/ic_outline_pending_actions_24"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        />
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="No bookings found"
        android:textSize="15sp"
        android:textColor="@color/white"
        android:visibility="gone"
        android:gravity="center"
        android:id="@+id/booking_noItemsFoundTextPendingAdmin"
        app:layout_constraintTop_toBottomOf="@+id/booking_noItemsFoundImgPendingAdmin"
        />

</androidx.constraintlayout.widget.ConstraintLayout>